// 1- integration with ai. -> create file inside /config to set credentials with .env.
// 2- create service to handle logic return response ai.
// 3- create controller to import logic from service.
// 4- i want load relation with user model and send to ai data's user and prompt static to ai of create cv.


----- header cv
full_name, email, phone, street, city. founded
not founed linkedin_url, summary, portfolio_link in jobSeekerProfile model.
User::->select(["id", "full_name", "email", "phone", "street", "city"])
->with([
    'jobSeekerProfile:id,linkedin_url,summary,portfolio_link',
    'skills:id,name',
    'educations:id,institution,degree,field_of_study,starts_at,ends_at',
    'experiences:id,company_name,title,starts_at,ends_at,description',
    'certificates:id,name,issuer,issued_at'
])
->get();
